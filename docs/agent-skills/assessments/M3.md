# M3 skill assessment

## Decision

**DO NOT CREATE**

Legacy/v0 normalization, lookup-table layout checks, canonical loaded-key
ordering, and arbitrary-byte robustness are Solana-specific implementation
details already covered by the existing `.claude/skills/solana-dev` guidance.
The new behavior is enforced by unit and property tests; duplicating it in a
new skill would not improve consistency.
