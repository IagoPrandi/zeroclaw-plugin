# M6 skill assessment

## Decision

**DO NOT CREATE**

The safety value is in deterministic code, rule IDs, precedence tests, and
`docs/RISK_RULES.md`. A prose skill cannot enforce block precedence or prevent
intent from relaxing hard caps. The existing Solana skill remains sufficient
for implementation guidance.
