# M5 skill assessment

## Decision

**DO NOT CREATE**

The milestone produced deterministic parsers and reconciliation functions, not
a reusable agent workflow. Tests cover signed SOL differences, raw token
amounts, fees, simulation failure, priority-fee rounding, and CPI stack
provenance. Maintaining those invariants in code is safer than restating them
in a skill.
